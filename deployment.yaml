apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: howest-fastapi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nathansegers/nginx-kubernetes
          ports:
            - containerPort: 80